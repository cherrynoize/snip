#########################################
###      snip configuration file      ###
#########################################

# default path for screenshots
export SNIP_PATH="$HOME/pictures/scrot"

# default path for screencasts
export SNIP_PATH_SCAST="$HOME/pictures/scast"

# default filename
#export SNIP_FILENAME="$(date '+%Y-%m-%d-%H%M%S')_"'$wx$h_'"$(hostname)-${USER}_snip"

# default image file format
#export SNIP_IMG_FILE=png

# default video file format
#export SNIP_VID_FILE=mp4

# default time delay before capture (in seconds)
#export SNIP_DELAY=0

# default command to execute after screenshots (compress png)
#export SNIP_SCREENSHOT_CMD="optipng \"%DEST%\" &"

# default command to execute after screencasts (strip audio)
#export SNIP_RECORD_CMD="ffmpeg -i \"%DEST%\" -c copy -an \"noa_%DEST%\" && mv \"noa_%DEST%\" \"%DEST%\"" # strip audio from video

# default tmp dir
#export SNIP_TMP_DIR=/tmp

# can be either 'true' or 'false'
#export SNIP_DEFAULT_FULLSCREEN=true

# either 0 or 1
#export SNIP_DEFAULT_VERBOSITY=1
